#include <stdio.h>
#include "Calculator.h"

int main_calculatormain()
{
	int x = 3, y = 4, result;
	count++;

	result = addition(x, y);

	printf("count = %d\n", count);
	printf("result = %d\n", result);

	return 0;
}

